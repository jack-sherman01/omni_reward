[project]
name = "omni_reward"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "gymnasium",
    "sentence-transformers",
    "torchvision",
    "openai",
]
